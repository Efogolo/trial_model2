[project]
name = "trial-conversion-model-2"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Efe Ogolo", email = "omohiefeogolo@email.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
trial-conversion-model-2 = "trial_conversion_model_2:main"

[build-system]
requires = ["uv_build>=0.12.16,<0.13.0"]
build-backend = "uv_build"
